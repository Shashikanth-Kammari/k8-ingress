kind: Deployment
apiVersion: apps/v1
metadata:
  name: app-1
  # these labels are deployment related labels
  labels:
    app: app-1
    purpose: ingress
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-1
      purpose: ingress
  # pod definition
  template:
    metadata:
      labels:
        app: app-1
        purpose: ingress
    spec:
      containers:
      - name: app1
        image: app1:1.0
---
apiVersion: v1
kind: Service
metadata:
  name: app-1
spec:
  selector:
    app: app-1
    purpose: ingress
  ports:
  - protocol: TCP
    port: 80 #service-port
    targetPort: 80 #target-port